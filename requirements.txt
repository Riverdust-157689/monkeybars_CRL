# Brachiation-on-G1 (monkeyBars_CRL) -- direct dependencies, pinned to the versions
# the current runs were produced with (Python 3.10.12, see docs/复现环境.md).
#
#   uv venv --python 3.10 .venv-warp
#   uv pip install --python .venv-warp/bin/python -r requirements.txt
#
# `mujoco` ships both `mujoco.mjx` and the bundled MJX-Warp backend; `warp-lang` is
# what actually compiles the physics kernels (CUDA >= 12.3 for the GPU path).
jax==0.6.2
jaxlib==0.6.2
mujoco==3.13.0
mujoco-mjx==3.13.0      # separate distribution: provides mujoco.mjx (the warp
                        # backend comes from its [warp] extra; warp-lang is pinned
                        # below explicitly)
warp-lang==1.17.0
flax==0.10.7
optax==0.2.8
brax==0.12.1
numpy==1.26.4            # <2.0: gym 0.26 / brax 0.12 need numpy 1.x
scipy==1.15.3
gym==0.26.2
ml-collections==1.1.0
absl-py==2.5.0
orbax-checkpoint==0.11.39
etils==1.13.0
pillow==12.3.0           # render_policy.py frame writing
wandb==0.30.0            # --wandb (optional)
# optional: `uv pip install imageio imageio-ffmpeg` for mp4 output (the renderer
# falls back to GIF without it).
#
# The RL loss/agent code is NOT a pip package here: it lives in third_party/jaxgcrl
# (upstream JaxGCRL at commit 5a6e7a0 + patches/jaxgcrl_crl_losses.patch).  See
# tools/setup_env.sh.
